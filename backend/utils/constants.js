const COLORS = ['red', 'blue', 'green', 'yellow'];
const MOVE_TIME = 15000;
const GAME_TIME_LIMIT = 30 * 60 * 1000; // 30 minutes in milliseconds
module.exports = { COLORS, MOVE_TIME, GAME_TIME_LIMIT };